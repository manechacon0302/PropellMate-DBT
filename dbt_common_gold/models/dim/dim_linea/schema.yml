version: 2

models:
  - name: dim_linea
    description: "Tabla de silver"
    config:
      unique_keys:
        - IdLine
        - DateLineStart
      materialized: incremental_alsa
      alias: Al_T_Dim_Line
      cluster_by: ["modifdate", "validStartDate", "ValidEndDate", "DeleteDate"]
      modif_date: DATE_SUB(CURRENT_TIMESTAMP(), INTERVAL 3 DAY)
      version_date: CURRENT_TIMESTAMP()
      historic: True
      delete_strategy:
        type: target
      #delete_strategy:
      #  type: source
      #  source_unique_keys:
      #    - table_name: ["bq_dim_silver_com", "Al_T_Dim_Line"]
      #      delete_clause: Null
      #      unique_keys:
      #        - IdLine: IdLine
      #        - DateLineStart: DateLineStart
