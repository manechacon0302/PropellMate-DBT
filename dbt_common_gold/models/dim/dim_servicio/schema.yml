version: 2

models:
  - name: dim_servicio
    description: "Tabla de gold Servicio"
    config:
      materialized: incremental_alsa
      alias: Al_T_Dim_ServiceType
      unique_keys:
        - IdServiceType
      business_start_date: CURRENT_TIMESTAMP()
      cluster_by: ["modifdate", "validStartDate", "ValidEndDate", "DeleteDate"]
      modif_date: DATE_SUB(CURRENT_TIMESTAMP(), INTERVAL 3 DAY)
      version_date: CURRENT_TIMESTAMP()
      historic: True
      delete_strategy:
        type: target
      #delete_strategy:
      #  type: source
      #  source_unique_keys:
      #    - table_name: ["bq_dim_silver_com", "Al_T_Dim_ServiceType"]
      #      delete_clause: Null
      #      unique_keys:
      #        - IdServiceType: IdServiceType